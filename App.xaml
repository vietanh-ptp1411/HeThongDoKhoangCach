<Application x:Class="HeThongDoKhoangCach.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>

        <!-- ===== Bảng màu (theo mock BISG) ===== -->
        <SolidColorBrush x:Key="BgBrush" Color="#EEF2F7"/>
        <SolidColorBrush x:Key="HeaderBrush" Color="#1B3A6B"/>
        <SolidColorBrush x:Key="HeaderPillBrush" Color="#2C4F86"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#1F5FBF"/>
        <SolidColorBrush x:Key="ValueBrush" Color="#14304F"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#14304F"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#5A6B7F"/>
        <SolidColorBrush x:Key="TextDarkBrush" Color="#1B1F24"/>
        <SolidColorBrush x:Key="CardBorderBrush" Color="#D5DCE6"/>
        <SolidColorBrush x:Key="IconBoxBrush" Color="#EAF1FB"/>
        <SolidColorBrush x:Key="FieldBgBrush" Color="#F5F8FC"/>
        <SolidColorBrush x:Key="FieldBorderBrush" Color="#C9D3E0"/>
        <SolidColorBrush x:Key="TableHeaderBrush" Color="#1B3A6B"/>
        <SolidColorBrush x:Key="PassBgBrush" Color="#E9F7EC"/>
        <SolidColorBrush x:Key="PassBorderBrush" Color="#2EAD4B"/>
        <SolidColorBrush x:Key="PassTextBrush" Color="#1E9E3E"/>
        <SolidColorBrush x:Key="NgBgBrush" Color="#FDECEC"/>
        <SolidColorBrush x:Key="NgBorderBrush" Color="#D9342B"/>
        <SolidColorBrush x:Key="NgTextBrush" Color="#C62828"/>
        <SolidColorBrush x:Key="MeasuringBgBrush" Color="#FFF8E1"/>
        <SolidColorBrush x:Key="MeasuringBorderBrush" Color="#F9A825"/>
        <SolidColorBrush x:Key="MeasuringTextBrush" Color="#B7791F"/>
        <SolidColorBrush x:Key="NeutralIconBrush" Color="#9AA7B8"/>
        <SolidColorBrush x:Key="StartBrush" Color="#1E9E3E"/>
        <SolidColorBrush x:Key="StopBrush" Color="#D9342B"/>
        <SolidColorBrush x:Key="ResetBrush" Color="#2E5FBF"/>
        <SolidColorBrush x:Key="StatusGreenBrush" Color="#1E9E3E"/>
        <SolidColorBrush x:Key="StatusRedBrush" Color="#D9342B"/>
        <SolidColorBrush x:Key="SelectionBrush" Color="#E3ECF7"/>
        <SolidColorBrush x:Key="OkCellBrush" Color="#C6EFCE"/>
        <SolidColorBrush x:Key="OkCellTextBrush" Color="#006100"/>
        <SolidColorBrush x:Key="NgCellBrush" Color="#FFC7CE"/>
        <SolidColorBrush x:Key="NgCellTextBrush" Color="#9C0006"/>

        <!-- ===== Icon vector (hộp 24x24) ===== -->
        <StreamGeometry x:Key="IconHome">M3,11 L12,3 L21,11 V21 H14 V15 H10 V21 H3 Z</StreamGeometry>
        <StreamGeometry x:Key="IconCalendar">M4,5 H20 V21 H4 Z M4,9.5 H20 M8,3 V7 M16,3 V7</StreamGeometry>
        <StreamGeometry x:Key="IconClock">M12,3 A9,9 0 1 1 11.99,3 Z M12,7 V12 L15.5,14</StreamGeometry>
        <StreamGeometry x:Key="IconLoadcell">M4,3 H20 V6.5 H4 Z M9,6.5 H15 V17.5 H9 Z M4,17.5 H20 V21 H4 Z</StreamGeometry>
        <StreamGeometry x:Key="IconRuler">M2,8 H22 V16 H2 Z M5,8 V12 M8,8 V11 M11,8 V12.5 M14,8 V11 M17,8 V12 M20,8 V11</StreamGeometry>
        <StreamGeometry x:Key="IconClipboard">M7,5 H17 V21 H7 Z M9.5,3 H14.5 V6.5 H9.5 Z M10,11 H14 M10,14 H14 M10,17 H12.5</StreamGeometry>
        <StreamGeometry x:Key="IconCaliper">M3,4 H21 V8 H3 Z M4,8 V20 H8 V8 M12,8 V15 H16 V8</StreamGeometry>
        <StreamGeometry x:Key="IconTarget">M12,3 A9,9 0 1 1 11.99,3 Z M12,7 A5,5 0 1 1 11.99,7 Z M12,10.5 A1.5,1.5 0 1 1 11.99,10.5 Z</StreamGeometry>
        <StreamGeometry x:Key="IconShield">M12,2 L20,5 V11.5 C20,16.5 16.6,20.8 12,22 C7.4,20.8 4,16.5 4,11.5 V5 Z</StreamGeometry>
        <StreamGeometry x:Key="IconCheck">M7.5,12.5 L10.5,15.5 L16.5,9</StreamGeometry>
        <StreamGeometry x:Key="IconCross">M8,8 L16,16 M16,8 L8,16</StreamGeometry>
        <StreamGeometry x:Key="IconNote">M5,3 H15 L19,7 V21 H5 Z M15,3 V7 H19 M8,11.5 H16 M8,14.5 H16 M8,17.5 H12.5</StreamGeometry>
        <StreamGeometry x:Key="IconPlay">M7,4 L19,12 L7,20 Z</StreamGeometry>
        <StreamGeometry x:Key="IconStop">M6,6 H18 V18 H6 Z</StreamGeometry>
        <StreamGeometry x:Key="IconReset">M18.5,7.5 A8,8 0 1 0 19.6,14.5 M18.5,3.5 V7.5 H14.5</StreamGeometry>
        <StreamGeometry x:Key="IconSave">M4,4 H16.5 L20,7.5 V20 H4 Z M7.5,4 V9.5 H14 V4 M8,20 V14.5 H16 V20</StreamGeometry>
        <StreamGeometry x:Key="IconUpload">M12,17 V5 M7,10 L12,5 L17,10 M4,20 H20</StreamGeometry>
        <StreamGeometry x:Key="IconBars">M4,20 V12 H8 V20 Z M10,20 V5 H14 V20 Z M16,20 V9.5 H20 V20 Z</StreamGeometry>
        <StreamGeometry x:Key="IconReport">M6,3 H15 L19,7 V21 H6 Z M15,3 V7 H19 M9,12 H16 M9,15 H16 M9,18 H13</StreamGeometry>
        <StreamGeometry x:Key="IconUser">M12,4 A4,4 0 1 1 11.99,4 Z M4,21 C4,16.5 7.5,14 12,14 C16.5,14 20,16.5 20,21 Z</StreamGeometry>
        <StreamGeometry x:Key="IconScale">M12,3 V20 M6,20 H18 M12,6 H4 L2,13 H6 L4,6 M12,6 H20 L22,13 H18 L20,6</StreamGeometry>
        <StreamGeometry x:Key="IconPackage">M12,3 L21,7.5 V16.5 L12,21 L3,16.5 V7.5 Z M3,7.5 L12,12 L21,7.5 M12,12 V21</StreamGeometry>
        <StreamGeometry x:Key="IconBarcode">M3,5 V19 M6,5 V19 M9,5 V19 M11,5 V19 M14,5 V19 M17,5 V19 M19,5 V19 M21,5 V19</StreamGeometry>
        <StreamGeometry x:Key="IconGear">M12,8.5 A3.5,3.5 0 1 1 11.99,8.5 Z M12,2 L13.6,4.4 L16.4,3.6 L17.2,6.4 L20,7.2 L19.2,10 L21.6,11.6 L20,14 L21.2,16.6 L18.6,17.8 L18.4,20.6 L15.6,20.4 L14,22.8 L12,21.2 L10,22.8 L8.4,20.4 L5.6,20.6 L5.4,17.8 L2.8,16.6 L4,14 L2.4,11.6 L4.8,10 L4,7.2 L6.8,6.4 L7.6,3.6 L10.4,4.4 Z</StreamGeometry>

        <Style x:Key="IconStroke" TargetType="Path">
            <Setter Property="Stroke" Value="{StaticResource AccentBrush}"/>
            <Setter Property="StrokeThickness" Value="1.8"/>
            <Setter Property="StrokeLineJoin" Value="Round"/>
            <Setter Property="StrokeStartLineCap" Value="Round"/>
            <Setter Property="StrokeEndLineCap" Value="Round"/>
            <Setter Property="Fill" Value="Transparent"/>
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="IconFill" TargetType="Path">
            <Setter Property="Fill" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- ===== Khối / thẻ / chữ ===== -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12,10"/>
        </Style>

        <Style x:Key="IconBox" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource IconBoxBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Width" Value="52"/>
            <Setter Property="Height" Value="52"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
        </Style>

        <Style x:Key="CardTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource ValueBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="BigValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="40"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource ValueBrush}"/>
        </Style>

        <Style x:Key="MidValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="19"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource ValueBrush}"/>
        </Style>

        <Style x:Key="UnitText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
        </Style>

        <Style x:Key="TableHeaderText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="4,5"/>
        </Style>

        <Style x:Key="TableCellText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="Margin" Value="4,0"/>
        </Style>

        <Style x:Key="PlaceholderText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#9AA7B8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="FieldTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
            <Setter Property="Background" Value="{StaticResource FieldBgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource FieldBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NoteTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="6,0"/>
        </Style>

        <!-- ===== Nút ===== -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13.5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="{StaticResource HeaderBrush}"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.88"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.72"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StartButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{StaticResource StartBrush}"/>
        </Style>
        <Style x:Key="StopButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{StaticResource StopBrush}"/>
        </Style>
        <Style x:Key="ResetButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{StaticResource ResetBrush}"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="18,0"/>
        </Style>

        <!-- Nút trắng có viền (dãy nút chức năng, RESET histogram, nút phụ) -->
        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource ValueBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F3F6FA"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SelectionBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SmallButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
            <Setter Property="Height" Value="26"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="12,0"/>
        </Style>

        <Style x:Key="OutlineButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,0"/>
        </Style>

        <Style x:Key="HomeButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E3ECF7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderPill" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource HeaderPillBrush}"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="StatusSeparator" TargetType="Rectangle">
            <Setter Property="Width" Value="1"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Fill" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="Margin" Value="18,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="StatusText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Foreground" Value="{StaticResource ValueBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
        </Style>

        <!-- ===== Bảng kết quả (báo cáo) ===== -->
        <Style x:Key="ResultGridHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F3F6FA"/>
            <Setter Property="Foreground" Value="{StaticResource ValueBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
        </Style>

        <Style x:Key="ResultGridCell" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" Padding="8,0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SelectionBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ResultGridRow" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <Style x:Key="ResultGrid" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EDF1F6"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFBFD"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="RowHeight" Value="30"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="ColumnHeaderStyle" Value="{StaticResource ResultGridHeader}"/>
            <Setter Property="CellStyle" Value="{StaticResource ResultGridCell}"/>
            <Setter Property="RowStyle" Value="{StaticResource ResultGridRow}"/>
        </Style>

        <!-- ===== Bảng chỉnh sửa (màn cài đặt) ===== -->
        <Style x:Key="EditGridCell" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SelectionBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="EditGrid" TargetType="DataGrid">
            <Setter Property="CellStyle" Value="{StaticResource EditGridCell}"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F7F9FC"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="RowHeight" Value="30"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="ColumnHeaderStyle" Value="{StaticResource ResultGridHeader}"/>
        </Style>

    </Application.Resources>
</Application>
